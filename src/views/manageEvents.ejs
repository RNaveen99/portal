<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Manage Events</title>
    <% include meta-data%>
</head>

<body>
    <% include header %>
    <div class="container">
        <% data.forEach((ele) => { %>
        <div class="row">
            <div class="col s6">
                Information
            </div>
            <div class="col s6">
                <div class="switch">
                    <label>
                        Off
                        <input type="checkbox" value="<%=ele.eventName%>" <% if(ele.isLive) {%> checked <% }%>>
                        <span class="lever"></span>
                        Live
                    </label>
                </div>
            </div>
        </div>
        <%}); %>
    </div>
    <% include footer %>
</body>

</html>