<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <% include meta-data %>
    <title>SignUp</title>
</head>

<body>
    <% include header %>
    <div class="container">
        <div class="row">
            <form method="POST" action="/auth/signUp" class="col s10 m6 l6 push-s1 push-m3 push-l3 card-panel">
                <div class="center">
                        <h2>Sign Up </h2>
                </div>
                <div class="row">
                    <div class="input-field col s10">
                        <input type="text" name="name" id="name" class="validate" value="<%=tempUser.name %>" required>
                        <label for="name">Name</label>
                        <span class="helper-text red-text name-helper"><%=errorData.name%></span>
                    </div>
                    <div class="input-field col s2">
                        <i class="material-icons small name-icon"><%if(errorData.name){%>clear<%} else {%>check<%}%></i>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s10">
                        <input type="text" name="college" id="college" class="autocomplete validate" value="<%=tempUser.college%>" required>
                        <label for="college">College</label>
                        <span class="helper-text red-text college-helper"><%=errorData.college%></span>
                    </div>
                    <div class="input-field col s2">
                        <i class="material-icons small college-icon"><%if(errorData.college){%>clear<%} else {%>check<%}%></i>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s10">
                        <input type="number" name="number" id="number" class="validate" value="<%=tempUser.number%>" required>
                        <label for="number">Mobile Number</label>
                        <span class="helper-text red-text number-helper"><%=errorData.number%></span>
                    </div>
                    <div class="input-field col s2">
                        <i class="material-icons small number-icon"><%if(errorData.number){%>clear<%} else {%>check<%}%></i>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s10">
                        <input type="email" name="email" id="email" class="validate" value="<%=tempUser.email%>" required>
                        <label for="email">Email id</label>
                        <span class="helper-text red-text email-helper"><%=errorData.email%></span>
                    </div>
                    <div class="input-field col s2">
                        <i class="material-icons small email-icon"><%if(errorData.email){%>clear<%} else {%>check<%}%></i>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s10">
                        <input type="password" name="password" id="password" class="validate" value="<%=tempUser.password%>" required>
                        <label for="password">Password</label>
                        <span class="helper-text red-text password-helper"><%=errorData.password %></span>
                    </div>
                    <div class="input-field col s2">
                        <i class="material-icons small password-icon"><%if(errorData.password){%>clear<%} else {%>check<%}%></i>
                    </div>
                </div>
                <div class="row">
                    <div class="col s8 m4 l4">
                        <button class="btn waves-effect waves-light" type="submit" name="action">Sign Up
                            <i class="material-icons right">send</i>
                        </button>
                    </div>
                    <span class="col s12 m7 l8">Already have an account <a href="/auth/signIn">Sign In </a>here</span>
                </div>
            </form>
        </div>
    </div>
    <% include footer %>
    <script src="/js/signUp.js"></script>
</body>

</html>